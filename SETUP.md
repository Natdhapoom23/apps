# ติดตั้ง Workshop Live บน Vercel + Firebase

ระบบมีหน้ารวมแอปและเกมสดใน repository เดียวกัน ไม่ต้องสร้าง Next.js project ใหม่ หน้าเว็บใช้ HTML/CSS/JavaScript และ API ฝั่งเซิร์ฟเวอร์ใช้ Vercel Node.js Functions

## ทดลองทันทีในเครื่อง

```sh
npm install
npm run build
DEMO_MODE=true npm run dev
```

เปิด http://127.0.0.1:4174/games/live/ แล้วเลือกหน้าผู้สอน รหัสผ่านโหมดทดลองคือ `workshop-demo` (เปลี่ยนได้ด้วย ADMIN_PASSWORD)

โหมดทดลองเก็บข้อมูลใน `.local/workshop.json` ซึ่งไม่ถูกส่งขึ้น GitHub และไม่ถูกเผยแพร่ในเว็บ ข้อมูลยังอยู่เมื่อรีสตาร์ตเซิร์ฟเวอร์ และโหมดนี้ถูกปิดบังคับเมื่อรันบน Vercel โทรศัพท์ต้องใช้ Wi-Fi เดียวกับเครื่องและอนุญาตการเชื่อมต่อผ่าน firewall ใช้ลิงก์ Mobile ที่เซิร์ฟเวอร์แสดง / QR จากจอฉาย หากมี VPN หรือหลาย network interface ให้ตั้ง `PUBLIC_URL` เป็น URL LAN ที่มือถือเข้าถึงได้

โหมดทดลองแสดงข้อมูลด้วย polling ทุกวินาที ส่วน Firebase จริงใช้ REST Server-Sent Events ที่ path ข้อมูลสาธารณะและมี polling สำรองเมื่อ stream ขัดข้อง

## Firebase ที่เลือกไว้

- Project ID: `ai-test-24515`
- Database URL: `https://ai-test-24515-default-rtdb.asia-southeast1.firebasedatabase.app`
- [Firebase Console](https://console.firebase.google.com/project/ai-test-24515/overview)

### 1. ติดตั้ง Database Rules ก่อนเปิดใช้งาน

ใน Firebase Console > Realtime Database > Rules ให้นำเนื้อหา `firebase.rules.json` ไป Publish หลังตรวจว่าไม่มีแอปอื่นใช้ฐานข้อมูลนี้อยู่ เพราะ rules นี้ปฏิเสธการอ่านเขียน path อื่นทั้งหมด

ถ้ามีระบบอื่นใช้ฐานข้อมูลเดียวกัน ให้รวมเฉพาะ rules ของ `/rooms`, `/roomIndex`, `/limits` กับ rules เดิม และห้ามมี `.read: true` หรือ `.write: true` ที่ระดับ root หรือ parent เพราะสิทธิ์ parent จะเปิดข้อมูลเฉลยและคำตอบส่วนตัวด้วย

- เปิดอ่านเฉพาะ `rooms/<รหัสห้อง>/public` เพื่อให้จอสดและมือถือฟังข้อมูลได้
- ห้ามผู้ใช้เขียนฐานข้อมูลโดยตรง
- เฉลย คลังคำถาม คำตอบรายบุคคล และข้อมูลจำกัดคำขอ อ่านได้เฉพาะเซิร์ฟเวอร์
- projection สาธารณะมีชื่อ กลุ่ม และคะแนนของผู้เข้าอบรม ผู้ที่มีรหัสห้องสามารถอ่านได้ ไม่ควรใช้ชื่อหรือข้อมูลอ่อนไหว

ไฟล์ `firebase.json` เตรียมไว้สำหรับ deploy rules ด้วย Firebase CLI เช่นกัน แต่ยังไม่ได้ deploy ไปยังบัญชีจริงจากงานนี้

### 2. ตั้งค่า Environment Variables ใน Vercel

เปิด Project Settings > Environment Variables แล้วตั้งค่าทั้ง Production และ Preview ตามที่ต้องการ:

| ชื่อ | ค่า |
| --- | --- |
| `FIREBASE_PROJECT_ID` | `ai-test-24515` |
| `FIREBASE_DATABASE_URL` | URL ด้านบน |
| `FIREBASE_CLIENT_EMAIL` | `client_email` จาก Service Account |
| `FIREBASE_PRIVATE_KEY` | `private_key` จาก Service Account รองรับ newlines หรือ `\n` |
| `ADMIN_PASSWORD` | รหัสผ่านผู้สอนที่ตั้งเอง อย่างน้อย 8 ตัวอักษร ควรยาวและเดายาก |
| `SESSION_SECRET` | สตริงสุ่มอย่างน้อย 24 ตัวอักษร ใช้ลงลายเซ็น session |

รับ Service Account จาก Firebase Console > Project Settings > Service accounts > Generate new private key ให้เก็บไฟล์นี้เป็นความลับ **อย่า commit หรือวาง key ในแชต** เซิร์ฟเวอร์ต้องมีสิทธิ์อ่านเขียน Realtime Database ของโปรเจกต์นี้

สร้าง SESSION_SECRET ได้ด้วย `openssl rand -hex 32` แล้วนำไปใส่ใน Environment Variables โดยตรง

ตัวเลือกเพิ่มเติม:
- `SITE_URL`: origin หลักของเว็บสำหรับตรวจ Origin; หากตั้ง จะรับคำขอแก้ไขจาก origin นี้เท่านั้น ไม่เหมาะเมื่อ Preview ใช้คนละโดเมน
- `PUBLIC_URL`: origin ที่ต้องการใช้ใน QR/ลิงก์ เช่น `https://your-app.vercel.app` หากไม่ตั้งจะใช้ host ของคำขอ
- **อย่าตั้ง DEMO_MODE=true บน Vercel** ระบบไม่ใช้ฐานข้อมูลจำลองบน production

ไม่ต้องใช้ Firebase Web API Key หรือเปิด Firebase Authentication ในสถาปัตยกรรมนี้ ผู้เล่นรับ session เฉพาะห้องจาก API เมื่อเข้าร่วม ผู้สอนใช้รหัสผ่านส่วนกลาง Sessions มีอายุ 12 ชั่วโมง ใช้สำหรับผู้สอนกลุ่มเล็กที่แชร์รหัสกัน ไม่ใช่ระบบหลายองค์กร

### 3. ตั้งค่า Vercel

Import GitHub repository `Natdhapoom23/apps` แล้วใช้ Framework Preset: Other

`vercel.json` กำหนดไว้แล้ว:
- Install: npm install ตาม lockfile
- Build Command: `npm run build`
- Output Directory: `dist`
- API: `/api/game` (Vercel Function)

หากโปรเจกต์ Vercel เดิมตั้ง Output Directory เป็น `.` หรือปิด Build Command อยู่ ให้ล้าง override หรือเปลี่ยนตามข้างบน ห้ามเผยแพร่ราก repository เป็น static ทั้งหมด เพราะมีไฟล์เซิร์ฟเวอร์อยู่ด้วย

หลังตั้ง env ให้ Redeploy จึงจะใช้งานค่าล่าสุด การ push GitHub จะ deploy อัตโนมัติก็ต่อเมื่อได้เชื่อม Vercel project แล้ว

## ใช้งานจริง

1. ผู้สอนเปิด `/games/live/admin.html` ล็อกอิน แล้วสร้างห้อง
2. ตั้งชื่อห้อง กลุ่ม เวลา คะแนน และแก้คำถามเริ่มต้น 15 ข้อ
3. เพิ่มคำถามภาพด้วยลิงก์ HTTPS หรือวางไฟล์รูปใน `assets/` แล้วใช้ `/assets/ชื่อไฟล์.jpg` ระบุเฉลย แหล่งที่มา และคำอธิบายด้วย ตัวระบบไม่ได้ตัดสินเองว่าภาพจริงหรือ AI
4. เปิดจอฉายจากปุ่มบนหน้าผู้สอน ให้ผู้เข้าอบรมสแกน QR ใส่ชื่อที่ไม่ซ้ำและเลือกกลุ่ม
5. กดเริ่มข้อ ผู้เล่นเห็นเฉพาะตัวเลือกกับเวลาบนมือถือ คำถาม/ภาพอยู่บนจอหลัก
6. เปลี่ยนคำตอบได้จนหมดเวลา API ใช้เวลาที่เซิร์ฟเวอร์รับคำขอเป็นหลัก; เครื่องผู้เล่นไม่สามารถส่งเวลา/คะแนนเองได้
7. หมดเวลาแล้ว API ปฏิเสธคำตอบทันที แม้หน้าผู้สอนปิดไป ไม่ต้องพึ่งตัวนับบนมือถือ ผู้สอนกดเฉลยเพื่อเผยคะแนน หรือปิดรอบก่อนเวลาก็ได้
8. เลือกข้อใหม่ หรือจบกิจกรรม แล้วดาวน์โหลดคะแนน CSV

ตัวจับเวลาหน้าจอใช้เวลาเซิร์ฟเวอร์เพื่อปรับนาฬิกา แต่เวลาตอบยังรวมความหน่วงของอินเทอร์เน็ต จึงไม่ใช่เครื่องมือวัดปฏิกิริยาที่แม่นยำระดับมิลลิวินาที

คำถามและกติกาข้อที่เริ่มแล้วถูกคัดลอกเก็บเป็นรอบ ไม่เปลี่ยนตามการแก้คลังย้อนหลัง การกดส่งคำตอบเดิมซ้ำไม่เพิ่มจำนวนเปลี่ยนใจหรือเลื่อนเวลาตอบ โบนัสเร็วคำนวณจากคำตอบสุดท้ายที่รับสำเร็จ ไม่ใช่ครั้งแรกที่กด

คะแนนเริ่มต้น: ถูก 1,000 + เร็วสูงสุด 500 + ไม่เปลี่ยนใจ 100; ผิด/ไม่ตอบได้ 0; คำถามความคิดเห็นไม่มีคะแนน คะแนนกลุ่มเป็นค่าเฉลี่ยสมาชิกทั้งหมดรวมคนที่ไม่ตอบ จึงควรปิดรับผู้เล่นก่อนเริ่มแข่งขันถ้าต้องการตรึงสมาชิก

## ขอบเขตและการทดสอบ

- `npm test` ทดสอบสิทธิ์ การปิดรับ เฉลย คะแนน การเปลี่ยนใจ ส่งซ้ำ คำถามความคิดเห็น และการเข้าร่วม/ตอบพร้อมกัน 100 ผู้เล่นในโหมดทดลอง
- **ผลทดสอบ 100 คนในเครื่องไม่ใช่ benchmark Firebase** ต้องทดสอบ Firebase จริงก่อนอบรม
- การแก้ไขแต่ละห้องใช้ Firebase transaction เพื่อรักษาความสอดคล้องและตัดสินคำตอบกับการปิดรอบพร้อมกัน ขณะตอบหนาแน่นอาจต้อง retry และต้องวัดเวลาตอบสนองจริง หากมีข้อผิดพลาดให้ผู้เล่นตรวจสถานะคำตอบและลองส่งอีกครั้งก่อนหมดเวลา
- Firebase RTDB Spark จำกัด 100 connections รวมผู้เล่น จอฉาย ผู้สอน และเซิร์ฟเวอร์ หากใกล้เพดานควรลดจำนวนอุปกรณ์หรือปรับแผน Firebase แต่ระบบนี้ไม่ได้เปลี่ยนแผนให้
- ก่อนเปิดระบบจริง API ตรวจว่าบุคคลภายนอกอ่าน path คำถามส่วนตัวไม่ได้ หากยังเปิด Test Mode แบบสาธารณะ ระบบจะไม่เริ่มทำงาน (ตรวจซ้ำอย่างน้อยทุก 60 วินาที)
- โหมดจริงต้องทดสอบ rules: อ่าน public ได้; อ่านทั้งห้อง/เฉลย/คำตอบไม่ได้; เขียนจาก client ไม่ได้
- QR จริงต้องเป็นโดเมน HTTPS ที่มือถือเข้าถึงได้ ทุกหน้ามีข้อความเมื่อการเชื่อมต่อขัดข้อง
- รูปตัวอย่างจริง/AI ยังไม่ได้ใส่ เพราะต้องใช้ภาพที่ผู้สอนทราบที่มาแน่ชัด ชุดเริ่มต้นเป็นคำถามข้อความและความคิดเห็น
- ยังไม่ได้ติดตั้ง rules, service account หรือ deploy Vercel จากเครื่องนี้

### เปลี่ยนรหัสผู้สอนจากหน้าเว็บ

หลังเข้าสู่ระบบที่ `/games/live/admin.html` ให้ใช้กล่อง **ความปลอดภัย** ในหน้ารวมห้อง กรอกรหัสเดิม รหัสใหม่ และยืนยันรหัสใหม่ (อย่างน้อย 8 ตัวอักษร) ระบบจะบันทึกรหัสใหม่ไว้ใน Firebase Realtime Database โดยไม่ต้องแก้โค้ดหรือเข้า Vercel อีกต่อไป
